name: Atualizar Ranking

on:
  schedule:
    - cron: '30 9 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  atualizar-ranking:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependências
        run: pip install requests beautifulsoup4

      - name: Executar scraper
        run: python scraper/buscar_dados.py

      - name: Commit e Push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add dados/ranking.json
          git diff --quiet && git diff --staged --quiet || git commit -m "🔄 Atualização do ranking - $(date +'%Y-%m-%d %H:%M')"
          git push
