name: Atualizar Ranking

on:
  schedule:
    - cron: '30 9 * * *'  # 6:30 Brasília (UTC-3)
  workflow_dispatch:

jobs:
  atualizar:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Configurar Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Instalar dependências
      run: pip install requests beautifulsoup4
    
    - name: Executar scraper
      run: python scraper/buscar_dados.py
    
    - name: Commit e push
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add dados/ranking.json
        git diff --staged --quiet || git commit -m "🔄 Atualização automática do ranking - $(TZ='America/Sao_Paulo' date +'%d/%m/%Y %H:%M')"
        git push
